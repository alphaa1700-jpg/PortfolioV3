version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies for packaging..."
      - npm install

  build:
    commands:
      - echo "No build step required for Node.js backend"

artifacts:
  files:
    - '**/*'
    - appspec.yml
    - scripts/**/*
  discard-paths: no
